# Changelog

Alle relevanten Änderungen und Weiterführungen dieses Forks.

## Unreleased
- Projektübernahme: Weiterführung und aktive Pflege durch neuen Maintainer.
- Neue Repository-Events implementiert: `created`, `deleted`, `renamed`, `archived`, `unarchived`, `edited`, `transferred`, `publicized`, `privatized`.
- API-Versionierung eingeführt: `/v1` (kompatibel) und `/v2` (hübschere Embed‑Variante, opt-in).
- Öffentliches Domain geändert zu `schlub.star-dev.xyz` und README entsprechend aktualisiert.
- Dokumentation: README auf Englisch aktualisiert, Beispiel für `/v2` Embeds hinzugefügt.

## 0.1.0 - initial
- Ursprüngliches Projekt: Baseline-Feature-Set (fork, issues, push, pull_request, watch, star).
